# Evan Tarot 網站分頁整理更新包

本次調整：

1. `index.html`：主頁只保留「介紹」與「預約」。
2. `lost-item.html`：新增失物占卜工具獨立分頁。
3. `services.html`：新增占卜項目獨立分頁。
4. `articles.html`：新增文章與留言區獨立分頁。
5. `timeflow.html`：保留占卜時間流獨立頁，並更新導覽列。

上傳方式：
- 將這些 HTML 檔放在 GitHub Pages 專案根目錄。
- 原本的 `style.css` 與 `JS/` 資料夾維持不動。
- 覆蓋既有的 `index.html` 與 `timeflow.html`。
- 新增 `lost-item.html`、`services.html`、`articles.html`。
